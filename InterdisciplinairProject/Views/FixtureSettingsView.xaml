<UserControl x:Class="InterdisciplinairProject.Views.FixtureSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:InterdisciplinairProject.Converters"
        Title="Fixture Settings" Height="450" Width="800">

    <Window.Resources>
        <converters:HexColorConverter x:Key="HexColorConverter"/>
    </Window.Resources>

    <Canvas Background="White" Margin="260,70,10,60">

        <!-- Title -->
        <TextBlock Text="{Binding FixtureName}" 
                   FontSize="18" 
                   FontWeight="Bold"
                   Canvas.Left="10" 
                   Canvas.Top="10"/>

        <!-- Border rond het slider gebied (was LightGreen achtergrond) -->
        <Border BorderBrush="Gray" 
                BorderThickness="2"
                CornerRadius="15"
                Canvas.Left="10" 
                Canvas.Top="50" 
                Width="500" 
                Height="280">

            <!-- Dynamic Sliders using ItemsControl -->
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Channels}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Border rond elke slider met afgeronde hoeken -->
                            <Border BorderBrush="LightGray" 
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Margin="5"
                                    Width="60">
                                <StackPanel Orientation="Vertical" 
                                            HorizontalAlignment="Center" 
                                            Margin="5,0">
                                    <!-- Channel Symbol -->
                                    <TextBlock Text="{Binding Symbol}" 
                                               HorizontalAlignment="Center"
                                               FontSize="32"
                                               FontWeight="Bold"
                                               Foreground="{Binding ColorHex, Converter={StaticResource HexColorConverter}}"
                                               Margin="0,0,0,5"/>

                                    <!-- Channel Name -->
                                    <TextBlock Text="{Binding Name}" 
                                               HorizontalAlignment="Center"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,5"/>

                                    <!-- Slider -->
                                    <Slider Minimum="0" 
                                            Maximum="255" 
                                            Orientation="Vertical"
                                            Value="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Height="160" 
                                            Width="20"/>

                                    <!-- Value Label -->
                                    <Label Content="{Binding Value}" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Orientation="Vertical"
                    Canvas.Right="10"
                    Canvas.Bottom="10">
            <Button Content="Save" Width="90" Height="25" Margin="0,0,0,8" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="90" Height="25" Click="CancelButton_Click"/>
        </StackPanel>

    </Canvas>
</Window>
