<UserControl x:Class="InterdisciplinairProject.Views.SceneEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:InterdisciplinairProject.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
            <TextBlock Text="Scène Naam:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding Scene.Name}" Width="200" Margin="0,0,10,0"/>
            <Button Content="Opslaan" Command="{Binding SaveSceneCommand}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
            <Button Content="Fixture Toevoegen" Command="{Binding AddFixtureCommand}" />
        </StackPanel>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListBox Grid.Column="0" ItemsSource="{Binding SceneFixtures}" SelectedItem="{Binding SelectedFixture}" Margin="5">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Fixture.Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <local:FixtureSettingsView Grid.Column="1" DataContext="{Binding FixtureSettingsViewModel}" Margin="5"/>
        </Grid>
    </Grid>
</UserControl>
