<UserControl x:Class="InterdisciplinairProject.Views.Scene.FixtureRegistryListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InterdisciplinairProject.Views.Scene"
             xmlns:converters="clr-namespace:InterdisciplinairProject.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="180">
    <UserControl.Resources>
        <converters:FixtureEndAddressConverter x:Key="EndAddressConverter"/>
    </UserControl.Resources>
    <Border Background="#FFE6D5E6" 
            CornerRadius="8" 
            BorderBrush="#FFCC99CC" 
            HorizontalAlignment="Stretch"
            BorderThickness="2"
            Margin="0">
        <DockPanel Margin="12">
            <!-- Header -->
            <TextBlock DockPanel.Dock="Top" 
                       Text="Fixture Inventaris" 
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,12"/>

            <!-- Import Button aan de onderkant -->
            <Button DockPanel.Dock="Bottom"
                    x:Name="ImportFixtureButton"
                    Content="Import Fixture to Registry"
                    Background="#FFCC99FF"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Padding="8,6"
                    Margin="0,12,0,0"
                    BorderThickness="0"
                    Cursor="Hand"
                    Command="{Binding ImportFixtureToRegistryCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFBB88EE"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Fixture List - AANGEPAST: MouseDoubleClick toegevoegd -->
            <ListBox x:Name="FixtureListBox"
                     BorderThickness="0"
                     Background="Transparent"
                     HorizontalContentAlignment="Stretch"
                     SelectionMode="Single"
                     ItemsSource="{Binding Fixtures}"
                     SelectionChanged="FixtureListBox_SelectionChanged"
                     MouseDoubleClick="FixtureListBox_MouseDoubleClick">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFFFFFFF"
                                CornerRadius="6"
                                Padding="10,8"
                                Margin="0,2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Fixture naam and DMX addresses -->
                                <StackPanel Grid.Column="0" Orientation="Vertical">
                                    <TextBlock Text="{Binding Name}"
                                               VerticalAlignment="Center"
                                               FontWeight="Medium"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="DMX: "
                                                   FontSize="11"
                                                   Foreground="Gray"/>
                                        <TextBlock Text="{Binding StartAddress}"
                                                   FontSize="11"
                                                   Foreground="Gray"/>
                                        <TextBlock Text=" - "
                                                   FontSize="11"
                                                   Foreground="Gray"/>
                                        <TextBlock FontSize="11"
                                                   Foreground="Gray">
                                            <TextBlock.Text>
                                                <Binding Converter="{StaticResource EndAddressConverter}"/>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Optionele delete/edit knoppen -->
                                <StackPanel Grid.Column="1" 
                                           Orientation="Horizontal"
                                           Visibility="Collapsed"
                                           x:Name="ActionButtons">
                                    <Button Content="✎" 
                                           Padding="4"
                                           Margin="4,0"
                                           Background="Transparent"
                                           BorderThickness="0"
                                           Cursor="Hand"
                                           ToolTip="Bewerken"/>
                                    <Button Content="✕" 
                                           Padding="4"
                                           Background="Transparent"
                                           BorderThickness="0"
                                           Cursor="Hand"
                                           ToolTip="Verwijderen"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="0"
                                            Margin="0,4">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </DockPanel>
    </Border>
</UserControl>