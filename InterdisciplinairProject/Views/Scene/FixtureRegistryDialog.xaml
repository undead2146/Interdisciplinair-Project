<Window x:Class="InterdisciplinairProject.Views.Scene.FixtureRegistryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:InterdisciplinairProject.Converters"
        xmlns:core="clr-namespace:InterdisciplinairProject.Core.Models;assembly=InterdisciplinairProject.Core"
        mc:Ignorable="d"
        x:Name="DialogRoot" Title="Fixture Registreren" Height="550" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">
    <Window.Resources>
        

        <!-- GridView Column Header Style -->
        <Style x:Key="ChannelHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#424242"/>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#BDBDBD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Focusable="false"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"
                                        Margin="12,0,12,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Drop Shadow Effect -->
    <Border Background="White" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#2196F3" CornerRadius="12,12,0,0" Padding="25,20" 
                    MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="Hand">
                <Grid>
                    <TextBlock FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center">
                        <Run Text="Fixture Registreren"/>
                    </TextBlock>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center"
                            Background="Transparent" 
                            Foreground="White" 
                            BorderThickness="0"
                            FontSize="18"
                            FontWeight="Bold"
                            Cursor="Hand"
                            Padding="8"
                            Click="CancelButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1976D2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0">
                <StackPanel Margin="30,25,30,30">

                    <!-- Fixture Name Display -->
                    <Border Background="#F5F5F5" CornerRadius="8" Padding="15,12" Margin="0,0,0,25">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🎭" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock VerticalAlignment="Center" FontSize="14">
                                <Run Text="Fixture:" FontWeight="SemiBold" Foreground="#666666"/>
                                <Run Text="{Binding SelectedFixture.Name}" FontWeight="Bold" Foreground="#212121"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Address Section -->
                    <TextBlock Text="DMX Adressen" FontSize="16" FontWeight="SemiBold" Foreground="#212121" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,25">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Start Adres" FontWeight="SemiBold" FontSize="13" Foreground="#666666" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding StartAddress, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource ModernTextBox}"
                                     Height="40"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Eind Adres" FontWeight="SemiBold" FontSize="13" Foreground="#666666" Margin="0,0,0,8"/>
                            <Border Background="#F5F5F5" 
                                    CornerRadius="6" 
                                    Height="40"
                                    BorderBrush="#DDDDDD"
                                    BorderThickness="1">
                                <TextBlock Text="{Binding EndAddress, Mode=OneWay}" 
                                           VerticalAlignment="Center"
                                           Padding="12,0"
                                           FontSize="14"
                                           Foreground="#666666"
                                           TextAlignment="Left"/>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- Instance Name Section -->
                    <TextBlock Text="Fixture Naam" FontSize="16" FontWeight="SemiBold" Foreground="#212121" Margin="0,0,0,12"/>
                    <TextBox Text="{Binding FixtureName, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"
                             Height="40"
                             Margin="0,0,0,25"/>

                    <!-- Channels Section -->
                    <TextBlock Text="Channels in Fixture" FontSize="16" FontWeight="SemiBold" Foreground="#212121" Margin="0,0,0,12"/>
                    <Border BorderBrush="#DDDDDD" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Background="White" 
                            Margin="0,0,0,25">
                        <ListView ItemsSource="{Binding SelectedFixture.Channels}" 
                                  MaxHeight="180" 
                                  BorderThickness="0"
                                  Background="Transparent">
                            <ListView.Resources>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"
                                                    HeaderContainerStyle="{StaticResource ChannelHeaderStyle}"/>
                                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="150"
                                                    HeaderContainerStyle="{StaticResource ChannelHeaderStyle}"/>
                                    <GridViewColumn Header="Min" DisplayMemberBinding="{Binding Min}" Width="80"
                                                    HeaderContainerStyle="{StaticResource ChannelHeaderStyle}"/>
                                    <GridViewColumn Header="Max" DisplayMemberBinding="{Binding Max}" Width="80"
                                                    HeaderContainerStyle="{StaticResource ChannelHeaderStyle}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Border>

                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Annuleren" 
                                Width="120" 
                                Height="42" 
                                Margin="0,0,12,0" 
                                Style="{StaticResource CancelButton}"
                                Click="CancelButton_Click"/>
                        <Button Content="Opslaan" 
                                Width="120" 
                                Height="42" 
                                Style="{StaticResource ModernButton}"
                                Click="SaveButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>