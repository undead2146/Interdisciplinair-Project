<Window x:Class="InterdisciplinairProject.Views.FixtureSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selecteer Fixture" Height="400" Width="500" 
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Selecteer een fixture om toe te voegen aan de scene:" 
                   TextWrapping="Wrap" 
                   FontWeight="SemiBold"/>

        <Border Grid.Row="2" 
                BorderBrush="#FFE0E0E0" 
                BorderThickness="1" 
                CornerRadius="4">
            <ListBox x:Name="FixtureListBox"
                     ItemsSource="{Binding}"
                     SelectionMode="Single"
                     BorderThickness="0"
                     Margin="4">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4">
                            <TextBlock Text="{Binding Name}" 
                                       FontWeight="SemiBold" 
                                       FontSize="14"/>
                            <TextBlock Text="{Binding Id}" 
                                       Foreground="Gray" 
                                       FontSize="11"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Width="100" 
                    Height="30"
                    Margin="6,0,0,0" 
                    IsDefault="True" 
                    Click="Ok_Click"
                    Content="Toevoegen"/>
            <Button Width="100" 
                    Height="30"
                    Margin="6,0,0,0" 
                    IsCancel="True" 
                    Click="Cancel_Click"
                    Content="Annuleren"/>
        </StackPanel>
    </Grid>
</Window>
