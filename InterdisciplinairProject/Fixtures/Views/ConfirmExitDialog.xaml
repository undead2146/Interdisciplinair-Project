<Window x:Class="InterdisciplinairProject.Fixtures.Views.ConfirmExitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Unsaved Changes"
        Height="150" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F0F0F0">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Name="MessageTextBlock"
                   TextWrapping="Wrap"
                   FontWeight="SemiBold"
                   FontSize="14"
                   Text="Changes have not been saved. What do you want to do?"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">

            <Button Content="Save" 
                    Width="100" 
                    Margin="5,0"
                    IsDefault="True"
                    Click="SaveButton_Click"/>

            <Button Content="Discard Changes" 
                    Width="140" 
                    Margin="5,0"
                    Click="DiscardButton_Click"/>

            <Button Content="Continue Editing" 
                    Width="150" 
                    Margin="5,0"
                    IsCancel="True"
                    Click="ContinueEditingButton_Click"/>

        </StackPanel>
    </Grid>
</Window>